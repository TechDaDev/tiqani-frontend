# Backup And Restore

Frontend has no persistent application data. Back up:

- source repository;
- build artifacts if artifact deployment is used;
- environment configuration in secret manager.

Database and media backup procedures live in backend `docs/BACKUP_AND_RESTORE.md`.
